server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: auth-backend
  
  datasource:
    url: jdbc:mysql://localhost:3306/busan?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: jin
    password: park2213
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        
  security:
    user:
      name: admin
      password: admin

logging:
  level:
    com.example.auth: DEBUG
    org.springframework.security: DEBUG
